#include <stdio.h>

// 특정 양의 수를 입력 받아 1부터 해당 수까지 3이라는 수는 몇 번 쓰게 되는지 계산하는 함수를 작성하시오
// (ex: 1539의 경우 정답은 514)

// ※주의※
// ㄱ. 0 ~ 9 : 3은 한 번
// ㄴ. 십의 자리에 3이 오는 경우 : 기본 1(일의 자리) + 10(십의 자리)
// ㄷ. 백의 자리에 3이 오는 경우 : 기본 1(일의 자리) + 10(십의 자리) + 100(백의 자리)
// .
// .
// .

void main(){
	int input_num;

	printf("1보다 큰 양의 정수 한 개를 입력하세요 : ");
	scanf("%d", &input_num);

	if(input_num < 10)
	
}
