#include <stdio.h>

void is_palindrome(char* arr, int size);

void main() {
	int counter;
	char word[50];
	

	for (counter = 0;counter!='\0';counter++) {
	}
}

void is_palindrome(char* arr, int size){


}