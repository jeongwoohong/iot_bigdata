#include <stdio.h>
#include <stdlib.h>
#pragma warning (disable :4996)

int compare_file(FILE* fFILE1, FILE* fFILE2) {

	char
}

void main(argc[],argv) {
	int read_file1;
	int read_file2;
	int index;

	compare_file(fopen(read_file1), fopen(read_file2));
}

